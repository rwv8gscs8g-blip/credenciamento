# Release Notes — V12.0.0083

## Resumo
- Objetivo: destravar a compilacao eliminando o identificador duplicado `VbMsgBoxResult` em relatorios.

## O que foi tentado e nao funcionou (rastreabilidade)
- `V12.0.0081` e `V12.0.0082` ainda nao compilaram por conflitos de identificadores (`VbMsgBoxResult`) em diferentes modulos.
- Esta iteracao remove o uso desse tipo no modulo de relatorios para permitir que o projeto compile e os testes possam voltar a rodar.

## Alteracoes
- **Central_Testes_Relatorio**: substituiu `Dim resp As VbMsgBoxResult` por `Long` (MsgBox retorna valores numericos) para evitar "Nome repetido encontrado: VbMsgBoxResult".

## Arquivos alterados
- `vba_export/Central_Testes_Relatorio.bas`
- `vba_export/Menu_Principal.frm`

## Status de compilacao (Excel)
- **COMPILOU** (VBE: Depurar > Compilar VBAProject).

## Testes realizados (Excel)
- Compilacao do projeto no Excel (sem execucao da bateria nesta iteracao).

## Assinatura
- Operador: (preencher)
- Data/Hora: (preencher)

