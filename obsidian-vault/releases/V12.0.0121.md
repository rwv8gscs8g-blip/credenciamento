# V12.0.0121

- Preencher: `AtualizarListaEmpresaMenuAtual` e `AtualizarListaEntidadeMenuAtual` passaram a priorizar os `TextBox` reais do designer (`TextBox17` e `TextBox16`) antes dos filtros dinâmicos legados.
- Menu_Principal: filtro de empresas ligado diretamente ao `TextBox17`, reduzindo dependência do binding automático e tornando o comportamento análogo ao já estabilizado em entidade e atribuição.
- Menu_Principal: mantido fallback para `TxtFiltroEmpresaDin` e `TxtFiltroEntidadeDin` apenas como compatibilidade.
