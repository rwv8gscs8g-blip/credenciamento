# V12.0.0176 — 2026-04-16

## Resumo

- **Rodizio / Atribuicao (Menu_Principal)**: remove dependencia de `TextBox18`/`TextBox22` (que podem “voltar” via `.frx`).
  - Nomes canonicos (designer): `TxtFiltro_Servico` e `TxtFiltro_EntidadeRodizio`.
  - Eventos passam a ser determinísticos via `mTxtFiltroServico_Change` e `mTxtFiltroRodizio_Change`.

## Arquivos

- `vba_export/Menu_Principal.frm`
- `vba_export/App_Release.bas`
