# V12.0.0091 — Relatório de bateria, impressão e RESULTADO_QA

## Objetivo

Corrigir falhas ao gerar o relatório da bateria (`RPT_BATERIA`), evitar perda do `Err` no tratamento de erro, remover geração duplicada/silenciosa do relatório no fim da bateria, esclarecer contadores nas mensagens, orientar suporte (e-mail + GitHub) e aplicar formatação inicial à aba `RESULTADO_QA`.

## Arquivos alterados

- `vba_export/Central_Testes_Relatorio.bas` — `CTR_GerarRelatorioBateria`: captura de erro antes de `Resume Next`/CSV; mensagem com `RESULTADO_QA`; ativação da aba tolerante a falha; impressão com `Util_PrepararAbaParaEscrita` / `Util_RestaurarProtecaoAba`; `Dim` no topo do procedimento.
- `vba_export/Teste_Bateria_Oficial.bas` — fim do bloco 5: não chama mais `CTR_GerarRelatorioBateria` duas vezes; logs BO_505/506; mensagens com contadores reais + suporte; `ClearContents` até coluna 11; `BA_InitExecucao` chama `CT_AplicarFormatoResultadoQA`; textos BO_500/501 com RESULTADO_QA.
- `vba_export/Central_Testes.bas` — `CT_AbrirResultadoQA` + `CT_AplicarFormatoResultadoQA`; navegação para primeira linha de dados; StatusBar explicando limite checklist ao vivo; MsgBox de bateria com RESULTADO_QA e suporte.
- `vba_export/Menu_Principal.frm` — `RELEASE_ATUAL` / `ITERACAO_ATUAL`.

## Compilação / testes

- **Compilou no VBA:** *(preencher após importar no Excel).*
- **Runtime sugerido:** executar bateria → Sim ao relatório → confirmar `RPT_BATERIA` preenchida; opcional imprimir; conferir formatação em `RESULTADO_QA`.

## Publicação

Rodar: `bash scripts/publicar_vba_import.sh`
