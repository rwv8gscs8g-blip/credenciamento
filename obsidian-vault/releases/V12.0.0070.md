# V12.0.0070 — Impressão: OS sem diálogo de impressora (impressora padrão)

## Resumo

Remove o bloqueio de UX na impressão de OS: a rotina `ImprimirOS` deixa de abrir o diálogo de setup (`xlDialogPrinterSetup`) e passa a imprimir direto na **impressora padrão do Windows**. Em caso de falha, exibe mensagem amigável com o erro.

## Alterações

- `vba_export/Preencher.bas`
  - `ImprimirOS`: remove `Application.Dialogs(xlDialogPrinterSetup).Show`; adiciona tratamento de erro e imprime via `ws.PrintOut`.

- `vba_export/Menu_Principal.frm`
  - `RELEASE_ATUAL` / `ITERACAO_ATUAL`: **V12.0.0070** / **12070**.

## Arquivos modificados

- `vba_export/Preencher.bas`
- `vba_export/Menu_Principal.frm`
- `release-notes/V12.0.0070.md`
- `vba_import/` (regenerado)

## Testes realizados (Excel)

- [ ] Reimport do módulo alterado (OK)
- [ ] Compilação do VBAProject (OK)
- [ ] Fluxo UI: Emitir OS → confirmar impressão → **não** aparece diálogo de impressora; imprime na padrão (OK)

## Assinatura

**Status**: Aguardando testes no Excel

