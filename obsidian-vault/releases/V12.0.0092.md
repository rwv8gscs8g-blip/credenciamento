# V12.0.0092 — Relatório RPT_BATERIA + busca Empresa/Entidade/Credencia

## Objetivo

1. Tornar a geração da aba `RPT_BATERIA` resiliente (ativação segura antes de excluir/recriar; fallback `Clear` se `Delete` falhar; última linha em `RESULTADO_QA` por colunas A e G).
2. Implementar busca nas listas do Menu Principal (empresas e entidades) e no formulário `Credencia_Empresa` (atividade/serviço), com parâmetros opcionais em `Preencher.bas`.

## Arquivos alterados

- `vba_export/Central_Testes_Relatorio.bas` — `CTR_CriarAbaRelatorio`, `ultLinha` em `CTR_GerarRelatorioBateria`, comentário CSV.
- `vba_export/Preencher.bas` — `PreenchimentoEmpresa`/`PreenchimentoEntidade`/`PreenchimentoCRServico` com filtro; helpers privados de texto para filtro.
- `vba_export/Menu_Principal.frm` — `mTxtFiltroEmpresa` / `mTxtFiltroEntidade` + `Filtros_CriarDinamico`; versão **V12.0.0092** / **12092**.
- `vba_export/Credencia_Empresa.frm` — `Option Explicit`, filtro dinâmico `CR_TxtFiltroListaDin` + `UserForm_Initialize`.
- `HANDOFF.md` — estado e ciclo de micro-iterações 4 h.

## Compilação / testes

- Compilar no VBA após importar `vba_import/`.
- Menu: abas com `M_Lista` e `C_Lista` — digitar em “Buscar empresa/entidade” e conferir filtro.
- Credenciamento: abrir credenciar — campo acima da lista filtra atividades/serviços.
- Bateria: gerar relatório — `RPT_BATERIA` preenchida sem erro.

## Publicação

`bash scripts/publicar_vba_import.sh`
